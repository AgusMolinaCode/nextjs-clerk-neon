/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'es6pwp1td3.ufs.sh',
      'img.clerk.com',
      'utfs.io',
      'images.unsplash.com',
      'assets.aceternity.com'
    ]
  }
}

export default nextConfig
